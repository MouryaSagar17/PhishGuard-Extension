fastapi>=0.110
uvicorn[standard]>=0.27
numpy>=1.24
pydantic>=2.0
# Needed to unpickle trained models (RandomForest, SVM pipeline, etc.)
scikit-learn>=1.3
# Only required if your champion model in phishing_model.pkl is XGBoost
xgboost>=2.0
